name: CodeQL

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  placeholder:
    name: CodeQL Placeholder
    runs-on: ubuntu-latest
    steps:
      - name: CodeQL Temporarily Disabled
        run: echo "CodeQL analysis temporarily disabled (repo rename runner issue). Will be re-enabled on or after March 19, 2026."
